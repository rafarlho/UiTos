/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface StatusGetAllGet$Params {

/**
 * Count
 */
  count?: boolean;

/**
 * Filter
 */
  filter?: string;

/**
 * OrderBy
 */
  orderby?: string;

/**
 * Skip
 */
  skip?: string;

/**
 * Top
 */
  top: string;
}

export function statusGetAllGet(http: HttpClient, rootUrl: string, params: StatusGetAllGet$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, statusGetAllGet.PATH, 'get');
  if (params) {
    rb.query('count', params.count, {});
    rb.query('filter', params.filter, {});
    rb.query('orderby', params.orderby, {});
    rb.query('skip', params.skip, {});
    rb.query('top', params.top, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

statusGetAllGet.PATH = '/Status/GetAll';
